Implementation is intentionally staged. This branch first lands deterministic diagnostics, selective-precision synthesis, and bounded research memory with tests. The continuous coordinator/worker scheduling hook should be added only after exact-head Security and Reliability confirms this core is safe and compatible with current main, so a scheduling mistake cannot destabilize the existing 17-worker research service.