The scheduled Anthropic-backed GitHub Actions definitions remain in the repository for safety/regression coverage, but both autonomous Anthropic runner configs are intentionally set to `"enabled": false`. This makes their planning step return `RUNNER_DISABLED` before any Anthropic model/API execution, because recurring paid Anthropic API usage was not authorized. Local Claude Code via the user's Claude subscription remains unaffected. Re-enable paid autonomous runners only with explicit owner approval and preserved project budget controls.
