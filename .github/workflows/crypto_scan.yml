name: Legacy Crypto Signal Scan (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Legacy signal generation is retired
        run: |
          echo "Legacy 15-minute signal/opportunity generation is retired."
          echo "Current priorities are profitable strategy discovery and 90-day 2x+ research."
          echo "No /scan or /evaluate request was sent."
