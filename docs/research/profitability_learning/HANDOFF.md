# PROFITABILITY-LEARNING-EVOLUTION-001 handoff

Role: direct user-assigned Work research engineering; separate from all active
canonical specialists. Branch: `agent/profitability-learning-evolution`.
Initial main: `c51a59dbac604a2e0b2eefa2b68ae025d4c5b0e3`.
Reconciled base: `bd44b9e957c915774833ccc11060548a5693ddf8`, incorporating PR #435.

## Implemented

Versioned economic contracts/schema; reconciled NAV analytics; money attribution;
concentration/catastrophe/cost diagnostics; development-only component ablation,
factorial interaction and multiplicity-corrected subgroup hypothesis generation;
immutable transactional memory; automatic successor drafts and frozen successor
validation; independent-event-aware component/family feedback; adaptive advisory
EXPLORE/EXPLOIT/LEARN missions; existing completion/factory/coordinator integration;
CLI completion and portable archive export/import.

No new paid dependency/service/model, no trading or promotion authority, no
protected OOS access, no canonical queue/rejected-registry mutation. Existing
heavy-worker eligibility and claims remain authoritative. No other PR's owned
director/research-runner files were changed; #387 compatibility is through callers.

## Verification and independent review

Baseline initially: 976 passed, 2 skipped. Rebased main included one additional
test. 77 new targeted tests cover the required failure/success, chronology,
attribution, identity, concurrency, memory and integration cases.

Independent read-only review of the initial implementation ran 65 tests and
reported five Important findings, no Critical findings. Reproduced 11 failing
regressions and repaired all five:

1. Component votes now exclude overlapping/relabelled development windows and
   respect their frozen sample floors.
2. Development independence is calculated separately from protected partitions.
3. Rejected exact fingerprints cannot contribute promising-family or EXPLOIT
   priority. Contradictory observations become diagnostic work only.
4. Archive events validate required fields/nested structures/authority before
   transaction commit. Malformed self-hashed payloads cannot poison the store.
5. Real legacy completions generate missing-evidence LEARN missions and apply
   bounded feedback to explicitly matched existing director/factory candidates.

An additional regression verifies configured memory outages block new candidate
dispatch instead of silently forgetting learned state. Configured corruption
never falls back to empty memory. Full applicable tests, Bandit, dependency audit,
secret scan and whitespace validation are required before PR publication; exact
final results and remote head/run are recorded in the PR body after they exist.

## Rulings and limits

- Implement the explicit user milestone continuously through tested PR handoff.
- Keep deployment activation separate from a claim of working market evidence.
  No strategy has been backtested or validated by these synthetic software tests.
- Missing rich portfolio artifacts cannot yield invented compounding, component
  validation, capacity or execution realism. Sparse legacy summaries are labelled.
- Native strategy evaluators must implement component omissions correctly; frozen
  hashes/metadata cannot independently authenticate their economic truth.
- Conditional permutation assumes block exchangeability; adjusted discovery
  evidence still requires fresh chronological replication and independent review.
- Exact economics/novelty of rule replacements requires independent review.
- Persistent SQLite must use an existing approved durable location; archive
  transfer must use an existing approved state channel. No new service provisioned.
- Historical provider provenance, causal validity, real profitability, deployment,
  OOS/forward performance and large-scale throughput were outside this code review;
  none is claimed. Canonical external gates remain required.

## Status and exact next action

IMPLEMENTED / locally TESTED. PR submission and exact-head remote CI are separate
states recorded in the PR. Not merged by this worker; not deployed; no new market
backtest, OOS, forward, cross-engine or validated-profitability evidence.

Independent Lead: review the final exact head and Security and Reliability result,
then integrate through the existing process if accepted. Next bounded task:
connect the newly frozen available-data strategy evaluator's real trade/NAV
artifact to `complete_experiment`, activate an existing approved durable memory
path/transport, and verify the deployed completion -> memory -> ranked-mission
chain. QUANT-004 and Big-Move remain independently owned.
