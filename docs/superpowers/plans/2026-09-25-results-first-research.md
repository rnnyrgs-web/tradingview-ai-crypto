# Results-first research execution plan

> **For agentic workers:** Use superpowers:executing-plans to implement this plan task-by-task. Execution is already requested by the user. No new permission prompt is needed for reversible research work; scientific admission must still be real.

**Goal:** Obtain a reproducible economic falsification or a legitimate screen survivor with the fewest outcome-independent steps, then advance only evidence that survives costs, simple controls and chronology.

**Architecture:** Use existing project gates and deterministic Python. A standalone fixed historical study may proceed only if its pre-outcome independent review establishes that it is permitted; the phrase research-only cannot substitute for admission. No worker, dashboard, schema or routing expansion.

**Tech Stack:** Existing Python runtime, standard library, NumPy already in repository requirements, provider historical archives, GitHub immutable commits and existing CI.

**Spec:** User results mission; `UNIFIED_PROFITABILITY_LEAD_SPEC.md`; `AGENTS.md`; exact `research_data/btc_weekly_persistence/protocol.json` for the proposed study.

## Global constraints

- Broker disconnected; trade/promotion authority OFF.
- One expensive deep strategy candidate at a time.
- Preserve chronology, point-in-time safety, realistic costs, robustness, multiple-testing controls, cross-engine checks where applicable, untouched OOS and genuine-forward gates.
- Never tune a failed rule, turn a control into a candidate after seeing its result, or rename a rejected mechanism.
- No paid-data purchase; no expansion of workers or model budget.
- Work on isolated branches; independent review and separate integration remain mandatory where canonical policy requires them.
- Record zero candidates if none qualifies. Historical data availability is not a 2x-prediction result.

## Current execution decision

Independent review blocked the proposed weekly screen on canonical admission, not market performance. Do not implement new schema infrastructure during this mission. Preserve the corrected hypothesis for later; focus current execution on the existing #797 -> #810 -> #790 closure path. #797 has now been separately Lead-integrated as main `1860f917c829ee5db4f4cd36404bf9702b6b680a` after existing independent bootstrap review and exact-head green CI. Duplicate #805 was closed without merge. Merged-main Security and Reliability36076911971 completed SUCCESS on that exact SHA.

## Decision priorities

1. **Existing ETH Tuesday:** shortest path if admission can be closed. #810 needs exact-head CI, legitimate separate integration into #790, new exact-head review, then a separately reviewed exact-identity admission patch. #797/#805 are overlapping reviewer-preflight fixes: integrate only the independently cleared chosen fix, never duplicate both. #727/#772 enforcement findings cannot be ignored because CI is green. Existing workflow dispatch is preferable to new code; authenticated dispatch is currently unavailable in this session. Preserve original source-content chronology limits on positive claims.
2. **One materially distinct available-data study:** propose BTC weekly own-trend persistence. It avoids the current intraday shock/lead-lag/calendar candidates and lowers trading frequency without selecting parameters from outcomes. It is a new rule motivated by published time-series momentum, not an exact paper reproduction. Resolve permission, rejected-memory novelty and scientific design *before* acquisition.
3. **2x historical cohort:** continue only nonduplicative evidence work. PR813's 100-file/91-fresh-pair source census cannot admit assets or snapshots. Next material need is independently authenticated historical membership/identity/tradability, not more downstream predictors. Existing #766/#778/#791 owners retain their work. No universe of today's survivors, invented inactive assets, price-only proxy for depth, or classification of missing outcomes as non-winners.

## Review focus

- Admission ambiguity: fail closed before any new historical outcome if independent review finds an applicable unfulfilled canonical boundary.
- Publication/selection chronology: source-inspired historical evidence is not exact replication, untouched OOS or a prospective record.
- Exposure mismatch: a long/cash strategy must beat simple beta controls, not just cash in a bull market.
- Dependence and concentration: nonoverlapping weeks are not IID; serial blocks, chronological halves and tail/winner vetoes matter.
- Archive execution realism: current archived opens/checksums cannot establish historical bid/ask fills or original publication vintage.

## Task 1 — freeze and independently assess the study

**Files:** `research_data/btc_weekly_persistence/protocol.json`; this plan.

- [x] Verify authoritative main, queues, rejected memory, open ownership and priority PR states.
- [x] Claim bounded issue #814; no duplicate of #806 or #790.
- [x] Write one exact mechanism, rule, market, window, cost ladder, baselines, failure gates and evidence classification.
- [ ] Freeze those files in a remote immutable GitHub commit before any data acquisition.
- [x] Obtain independent non-authoring scientific/policy review. Important findings must be resolved pre-outcome and frozen append-only or as a new pre-outcome revision; no return-driven choice.
- [x] If integrated admission is required, stop this experiment before data acquisition and preserve the exact blocker. Do not call a local review canonical admission.

## Task 2 — implement only the permitted deterministic evaluation

**Files if permitted:** `research_data/btc_weekly_persistence/run_study.py`, `tests/test_btc_weekly_persistence.py`.

**Consumes:** exact protocol; exactly 76 monthly BTCUSDT 1d archives, September2018–December2024, each with filename-bound provider SHA256.
**Produces:** per-week decisions and returns, exact raw/normalized/evaluator identities, partition/control/cost results, fixed falsifier verdict.

- [ ] Write synthetic tests for malformed/duplicate/gapped bars, lookahead mutation, fee arithmetic, partition boundaries, control formation and rejecting concentration/tail/half failures.
- [ ] Verify those tests fail before implementation, implement the literal protocol, and run the focused suite.
- [ ] Separate acquisition from evaluation. No production imports, provider substitutions or 2025+ URLs. Retain archive/checksum bytes and source URLs; fail on incomplete coverage.
- [ ] Freeze evaluator source hash in GitHub. Independent read-only review must cover code and any protocol correction before real outcomes.
- [ ] Run the existing full applicable suite and exact-head CI; no CI result grants economic admission.

Exact scheduled trade logic:

```python
signal = open_price[entry - timedelta(days=1)] / open_price[entry - timedelta(days=8)] - 1
active = signal > 0
net_return = (exit_price / entry_price) * (1 - cost_bps / 20000) / (1 + cost_bps / 20000) - 1
```

Entries are Mondays2019-01-07..2024-12-23; exits are exactly seven days later. Prices informing the signal are at least24h old. Consecutive active weeks are closed/reopened with a full round-trip cost; no fee-free stitching. No position is short, leveraged or funded. Exclude2021-12-27 and2023-06-26 entries because their exits cross frozen partition boundaries. Require positive compounded net return, not only positive arithmetic expectancy, in training, validation and both validation halves.

## Task 3 — execute once and make a binding research decision

**Files if permitted:** immutable acquisition manifest, raw archives, `results.json`, per-week CSV, `RESULTS_REPORT.md`.

- [ ] Acquire only the frozen data after both prior tasks pass; independently check checksums and date coverage.
- [ ] Execute once, training and then validation in chronological order with no intervening rule change. A replay verifies reproducibility, not a new test.
- [ ] Report active sample counts, mean gross/net expectancy, PF, win/loss distribution, weekly-marked drawdown, calendar means, train/validation halves, known-at-entry regimes,24/48/72bps sensitivity and winner/tail concentration.
- [ ] Report all fixed baselines and paired calendar-week differences. Compute fixed4-week block-bootstrap descriptive intervals; do not claim a new alpha budget.
- [ ] If an economic gate fails, preserve exact failure and mechanism learning; do not optimize signal horizon, entry weekday, threshold or periods.
- [ ] If the screen survives, STOP broad search. Request canonical single-candidate admission, freeze a genuinely untouched test and realistic execution contract, then walk-forward/regime, robustness, cross-engine and matured forward validation. No profitable label before earned evidence.
- [ ] If data or sample power fails, report inconclusive, not rejected profitability or a pass.

## Task 4 — advance the 2x lane only with authentic inputs

**Existing work:** #505/#514/#519, #766/#778/#791 and PR813; no new downstream schema.

- [ ] For a fixed historical decision date, independently qualify canonical identity, membership, failed/delisted coverage, listing age, price, liquidity and required tradability fields.
- [ ] Only when coverage passes, freeze the exact whole cohort and outcome-blind matched-control selection before labels.
- [ ] Generate genuine <=90-day >=100% events with start/cutoff/reference prices and immutable provenance; do not count daily overlapping labels as independent events.
- [ ] Freeze one economically grounded precursor and estimate base rate, lift, precision@K, false positives, prospective return and adverse excursion. PR-AUC only when powered.
- [ ] Only a predeclared qualifying current asset gets an immutable pre-move prospective record. Otherwise report NO QUALIFIED CURRENT 2X CANDIDATE.

## Resource and stopping decisions

Nominal75% strategy,25% 2x; blocked work yields to the next legitimate evidence task. Generic engineering only for a concrete research blocker. Prefer standard-library/NumPy execution over model calls. A reviewer rejection, failed economic test or missing historical field is useful information; an additional abstract module is not the objective. If both lanes require an external action, report the exact action and preserve a ready-to-run frozen package rather than inventing an exception.

## Execution record

- Main55e3ad02c3f050510c621cc04616c1112fe2b4a6 unchanged at restart.
- #790/#810 admission remains closed; #806 is actively owned elsewhere.
- No new market prices/outcomes have been acquired while this plan is drafted.

## Decisions from independent review

- Admission requires new reviewed canonical behavior/value/data-projection support; no local exception or TEST-schema workaround is allowed. Current study is BLOCKED, not a market rejection.
- Cross-boundary weeks excluded before outcomes; this changes no observed result because none was acquired.
- Positive compounded NAV added to survivor criteria to prevent positive-average/negative-wealth false passes.
- Shared ancestry with existing moderate-volatility own-return continuation retained for multiplicity; it is not an unrelated statistical family.
- Random50% placebo is not exact exposure matching; training-frozen beta control and validation exposure reporting are separately required.
- Archive vintage, executable fills, capacity, original source bytes and actual sample/economics remain unknown.

## Concrete next user-only operation

The GitHub connector can read runs and merge legitimately reviewed PRs, but has no workflow-dispatch action; the in-app browser was signed out. To generate the missing exact-head CI without changing workflows, use the existing Security and Reliability workflow at:
https://github.com/rnnyrgs-web/tradingview-ai-crypto/actions/workflows/security.yml

Select Run workflow, branch `review/pr790-stage1-admission-gate`, then Run workflow. Expected source head: `78c97f61b1c62f663b62ac4215821227d5532a65`. Equivalent authenticated local command:

```sh
gh workflow run security.yml --repo rnnyrgs-web/tradingview-ai-crypto --ref review/pr790-stage1-admission-gate
```

This runs tests only and does not open outcomes. Once exact-head CI succeeds, a separate Lead may integrate the already independently reviewed #810 repair, reconcile #790 with the new main, and issue one fresh exact-head scientific-review request. The issue-triggered queue is already available on main; no new orchestrator is needed. The later exact-identity admission patch remains separately reviewed and integrated before Stage1.
