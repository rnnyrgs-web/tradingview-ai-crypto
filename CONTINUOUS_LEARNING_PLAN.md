# Continuous Learning Integration Plan

1. Consume only genuinely resolved prediction-ledger rows.
2. Diagnose recurring wrong-signal groups by horizon, regime, direction, score band and immutable strategy identity.
3. Reuse the fixed selective-precision measurement layer without choosing production thresholds from forward outcomes.
4. Persist only compact advisory research lessons, bounded and deduplicated by fingerprint.
5. Feed the resulting priorities to a lightweight continuous research role and the AI quant-development cycle.
6. Never mutate production strategy behavior automatically. Every proposed improvement must enter the normal challenger and validation pipeline.
7. Do not increase paid compute or heavy-worker concurrency.
